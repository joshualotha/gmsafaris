# SEO Audit Log

## 2026-05-05 — Initial Git Push to GitHub

**What was done:**
- Initialized a Git repository in `gmsafaris-laravel/`
- Created `.gitignore` excluding `/vendor/`, `/node_modules/`, `.env`, `.DS_Store`, cache files, and logs
- Added all project files (code, views, assets, images, database, config) to the repo
- Pushed to remote: `https://github.com/joshualotha/gmsafaris.git` (branch: `main`)
- Force-pushed to overwrite any existing remote content with the full Laravel project

**What changed:**
- Repository now contains the complete `gmsafaris-laravel` project (1,047 files, ~46k lines of code)
- Remote `origin/main` points to commit `ccf384e` — "Initial commit: Golden Memories Safaris Laravel project"
- All images, Blade templates, controllers, migrations, routes, and config are included

**Expected impact:**
- Project is now backed up and version-controlled on GitHub
- Enables collaboration, CI/CD, and deployment workflows
- SEO audit report (`.seo-audit-report.md`) and context (`.seo-context.md`) are tracked alongside the codebase

## 2026-05-06 — Restructure for cPanel Deployment (public/ → Root)

**What was done:**
- Moved `public/index.php` → root `index.php` and updated all `__DIR__.'/../'` paths to `__DIR__.'/'`
- Moved `public/.htaccess` → root `.htaccess` (replaced old static HTML `.htaccess`)
- Moved `public/css/`, `public/js/`, `public/lib/`, `public/img/` directories to root level
- Removed the `public/` directory entirely
- Updated `config/filesystems.php` symlink path from `public_path('storage')` to `base_path('storage/app/public')`
- Committed as `e0c6447` and pushed to `origin/main`

**What changed:**
- cPanel document root (typically `public_html`) now directly serves `index.php` and `.htaccess` at the project root
- Laravel's front controller (`index.php`) now references `vendor/`, `bootstrap/`, `storage/` via `__DIR__.'/'` instead of `__DIR__.'/../'`
- Static assets (`css/`, `js/`, `lib/`, `img/`) are accessible directly from the document root
- The `public/` subdirectory no longer exists in the repository

**Expected impact:**
- Resolves the 403 Forbidden error caused by cPanel's document root pointing to the project root instead of a `public/` subdirectory
- Future `git push` from local and `git pull` on cPanel will sync cleanly without conflicts
- No further restructuring needed — the repo is now compatible with cPanel's default document root setup

## 2026-05-06 — Add vendor/ to Repo for cPanel Compatibility

**What was done:**
- Removed `/vendor/` from `.gitignore` so Composer dependencies are tracked in git
- Regenerated `composer.lock` with PHP 8.2 compatible package versions (cPanel runs PHP 8.2.30)
- Added all `vendor/` files to the repository
- Updated `.seo-audit-log.md`

**What changed:**
- `vendor/` directory is now tracked in git and will be pushed/pulled with the repo
- `composer.lock` updated to use package versions compatible with PHP 8.2
- `.gitignore` no longer excludes `/vendor/`

**Expected impact:**
- cPanel server no longer needs Composer installed — `vendor/` comes with the git pull
- After `git reset --hard origin/main` on cPanel, `vendor/autoload.php` will exist and Laravel will boot
- 500 Internal Server Error should be resolved

## 2026-05-06 — Fix SQLite Database Path for Portability

**What was done:**
- Changed `DB_DATABASE` in `.env.example` from absolute local path (`/Users/joshcodes/Documents/gms/gmsafaris-laravel/database/database.sqlite`) to relative path (`database/database.sqlite`)
- Committed as `2d57134` and pushed to `origin/main`

**What changed:**
- `.env.example` now uses a portable relative path that works on any server
- New developers or deployments that copy `.env.example` to `.env` will get a working database path
- The `config/database.php` fallback `database_path('database.sqlite')` already resolves correctly to the project-relative path

**Expected impact:**
- cPanel `.env` needs to be updated to use `DB_DATABASE=database/database.sqlite` instead of the old absolute path
- After updating `.env` and clearing config cache, the SQLite database will be found at the correct location
- Resolves the `SQLiteDatabaseDoesNotExistException` error on cPanel

## 2026-05-06 — Fix Owl Carousel jQuery Compatibility (Hero Carousel)

**What was done:**
- Changed Owl Carousel script source from local asset (`lib/owlcarousel/owl.carousel.min.js`) to CDN (`https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js`)
- The local file was correct (uses `addBack` not `andSelf`), but cPanel had a stale copy from the old static HTML site that used the deprecated `.andSelf()` method removed in jQuery 3.x
- Committed as `7adbdd8` and pushed to `origin/main`

**What changed:**
- `resources/views/partials/scripts.blade.php` now loads Owl Carousel from Cloudflare CDN instead of local asset
- The CDN version is guaranteed to be the official v2.3.4 release compatible with jQuery 3.x

**Expected impact:**
- Hero carousel and testimonial carousels will initialize correctly
- Resolves `TypeError: t(...).find(...).andSelf is not a function` error
- CDN loading also improves page load speed (cached across sites)

## 2026-05-06 — Fix Broken Contact/Inquiry Forms Across All Pages

**What was done:**
- Wired all 8 broken contact/inquiry forms to the existing `InquiryController@store` via `route('inquiry.store')`
- Added `@csrf` protection to all forms (was completely missing)
- Renamed form field `name` attributes to match the `Inquiry` model fillable fields: `name`, `email`, `phone`, `country`, `subject`, `message`
- Added hidden `subject` fields with descriptive default values per page type (e.g., "Luxury Safari Inquiry", "Kilimanjaro Route Inquiry")
- Updated JavaScript AJAX fetch URLs from non-existent `process-form.php` to `{{ route('inquiry.store') }}`
- Added CSRF token header to JavaScript fetch requests
- Added `<meta name="csrf-token" content="{{ csrf_token() }}">` to `layouts/app.blade.php` for JS CSRF support
- Added missing `phone` and `country` fields to the main contact form

**Pages fixed:**
1. `resources/views/contact.blade.php` — action from `process-form.php` → route, added `@csrf`, phone/country fields, JS fetch URL + CSRF header
2. `resources/views/tailoredsafaris.blade.php` — action from `#` → route, added `@csrf`, renamed fields
3. `resources/views/luxurysafari.blade.php` — action from `#` → route, added `@csrf`, renamed fields, added subject
4. `resources/views/mountaintrekking.blade.php` — added missing action, added `@csrf`, renamed fields, added subject
5. `resources/views/groupsafaris.blade.php` — action from `#` → route, added `@csrf`, renamed fields, added subject
6. `resources/views/budgetsafari.blade.php` — action from `#` → route, added `@csrf`, renamed fields, added subject
7. `resources/views/zanzibarbeachholiday.blade.php` — added missing action, added `@csrf`, renamed fields, added subject, updated JS URL
8. `resources/views/kilimanjaroroutes.blade.php` — added missing action, added `@csrf`, renamed fields, added subject, updated JS URL

**What changed:**
- All 8 forms now submit to `POST /inquiry` with proper CSRF protection
- Form field names now match the `Inquiry` model's `$fillable` attributes, so `$request->validated()` maps correctly
- Each form type sends a distinct `subject` value for easy categorization in the admin panel
- The layout now includes a CSRF meta tag for JavaScript-based submissions

**Expected impact:**
- Contact/inquiry forms now function correctly — submissions reach the database and trigger email notifications (admin alert + customer auto-reply)
- Resolves Critical issue #1.4 from the SEO audit report (Broken Contact Forms — 🚫 NON-FUNCTIONAL)
- Business can now receive leads through all inquiry channels
- Forms are properly secured against CSRF attacks

## 2026-05-06 — Fix Remaining Broken Form (booking.blade.php) + AJAX JSON Support

**What was done:**
1. **Fixed `resources/views/booking.blade.php`** — the last page still referencing `process-form.php`:
   - Added `action="{{ route('inquiry.store') }}"` and `@csrf` to the form
   - Added `name` attributes to all form inputs (previously only had `id` attributes, so form data would not serialize)
   - Updated AJAX URL from `process-form.php` to `{{ route('inquiry.store') }}`
   - Fixed Privacy Policy link from `#` to `{{ route('privacypolicy') }}`
   - Made the `comments` textarea `required` (maps to `message` field) and renamed to `message`

2. **Updated `app/Http/Controllers/InquiryController.php`** — added JSON response support:
   - Added `$request->expectsJson() || $request->ajax()` check
   - Returns `response()->json(['success' => true, 'message' => ..., 'inquiry_name' => ...])` for AJAX requests
   - Standard form submissions still redirect to the thank-you page
   - This fixes the AJAX-based forms on `contact.blade.php`, `kilimanjaroroutes.blade.php`, `zanzibarbeachholiday.blade.php`, and `booking.blade.php`

**What changed:**
- `booking.blade.php` — form now submits to `POST /inquiry` with CSRF protection and proper field names
- `InquiryController.php` — now handles both standard POST and AJAX POST requests correctly

**Expected impact:**
- **All 9 inquiry forms on the site now function** (contact, tailoredsafaris, luxurysafari, mountaintrekking, groupsafaris, budgetsafari, zanzibarbeachholiday, kilimanjaroroutes, booking)
- AJAX-based forms no longer get a redirect response — they receive JSON and show success/error messages via SweetAlert or alert()
- Resolves the last remaining `process-form.php` reference in source code
- Business can now capture leads from ALL pages, not just the contact page

## 2026-05-06 — Configure SMTP Email for Live Server (cPanel)

**What was done:**
1. **Created `config/mail.php`** — the mail configuration file was never published in this Laravel 10 project. Created with:
   - Default mailer set to `smtp` (reads from `MAIL_MAILER` env)
   - SMTP mailer configured with `host`, `port`, `encryption`, `username`, `password` from env
   - Global `from` address set to `info@gmsafaris.co.tz`
   - All standard mailers defined (smtp, ses, mailgun, postmark, sendmail, log, array, failover)

2. **Updated `.env`** — changed from local dev (`MAIL_MAILER=log`) to live SMTP:
   - `MAIL_MAILER=log` → `MAIL_MAILER=smtp`
   - `MAIL_HOST=127.0.0.1` → `MAIL_HOST=mail.gmsafaris.co.tz`
   - `MAIL_PORT=1025` → `MAIL_PORT=465`
   - `MAIL_USERNAME=null` → `MAIL_USERNAME=joshualotha@gmsafaris.co.tz`
   - `MAIL_PASSWORD=null` → `MAIL_PASSWORD=[set]`
   - `MAIL_ENCRYPTION=null` → `MAIL_ENCRYPTION=ssl`
   - Added `MAIL_ADMIN_ADDRESS="info@gmsafaris.co.tz"`

3. **Updated `.env.example`** — added `MAIL_ADMIN_ADDRESS` placeholder and corrected default SMTP host/port/encryption for consistency

**What changed:**
- `config/mail.php` — **NEW FILE** — provides the full Laravel 10 mail configuration with SMTP defaults
- `.env` — mail settings now point to the live cPanel mail server with SSL encryption
- `.env.example` — now includes `MAIL_ADMIN_ADDRESS` and corrected SMTP defaults

**Expected impact:**
- Inquiry form submissions will now send real emails via the cPanel SMTP server (`mail.gmsafaris.co.tz`)
- Admin receives new inquiry notifications at `info@gmsafaris.co.tz`
- Customers receive auto-reply confirmations from `info@gmsafaris.co.tz`
- Resolves the email delivery issue — emails no longer go to log files
- **Important**: This `.env` change is for the local development environment. On the live cPanel server, the `.env` file must also be updated with these same SMTP credentials. The `.env` file is NOT tracked in git (it's in `.gitignore`), so it must be updated manually on cPanel via `git pull` + editing `.env`, or via cPanel's file manager.

## 2026-05-06 — Upgrade Email Templates to Branded HTML (Admin + Customer)

**What was done:**
- Rewrote both email templates in `InquiryController::sendInquiryNotifications()` from plain inline HTML to fully styled, responsive email HTML with:
  - Dark navy header with gold accent branding (`#1a1a2e` / `#C8A45C`)
  - Admin notification: structured table layout with labeled fields, gold alert banner ("Action Required — Respond within 24 hours"), highlighted message block with left gold border
  - Customer auto-reply: warm thank-you header with emoji, gold CTA banner ("We will contact you within 24 hours"), inquiry summary card, numbered next-steps list, contact details with icons, professional footer
  - All styles are inline (required for email client compatibility — Gmail, Outlook, Apple Mail)
  - All user content is properly escaped with `e()` to prevent XSS

**What changed:**
- `app/Http/Controllers/InquiryController.php` — both `Mail::html()` calls now use full HTML document templates with inline CSS

**Expected impact:**
- Admin receives professional, scannable notification emails — easier to read on mobile, clear call to action
- Customer receives a polished, trust-building auto-reply that reinforces brand quality (E-E-A-T signal)
- Reduces spam complaints — branded emails are less likely to be marked as spam
- Improves customer confidence immediately after form submission (reassurance + clear next steps)
